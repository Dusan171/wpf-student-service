<Window x:Class="StudentService.Gui.UpdateAdress"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:StudentService.Gui"
        mc:Ignorable="d"
        Title="UpdateAdress" Height="450" Width="800">
    
    <Window.Resources>
        <!-- Style for Labels -->
        <Style TargetType="Label">
            <Setter Property="Margin" Value="0,5,0,5"/>
        </Style>
        <!-- Style for TextBoxes -->
        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="100,5,0,5"/>
            <Setter Property="MinWidth" Value="200"/>
        </Style>
        <!-- Style for Button -->
        <Style TargetType="Button">
            <Setter Property="Width" Value="120"/>
            <Setter Property="Margin" Value="10,20,0,0"/>
            <Setter Property="VerticalAlignment" Value="Top"/>
        </Style>
    </Window.Resources>
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Label Content="Id:" Grid.Row="0"/>
        <TextBox x:Name="IdTextBox" Text="{Binding Id, UpdateSourceTrigger=PropertyChanged}" Grid.Row="0" />

        <Label Content="Street:" Grid.Row="1"/>
        <TextBox x:Name="StreetTextBox" Text="{Binding Street, UpdateSourceTrigger=PropertyChanged}" Grid.Row="1" />

        <Label Content="Number:" Grid.Row="2"/>
        <TextBox x:Name="NumberTextBox" Text="{Binding Number, UpdateSourceTrigger=PropertyChanged}" Grid.Row="2" />

        <Label Content="Town:" Grid.Row="3"/>
        <TextBox x:Name="TownTextBox" Text="{Binding Town, UpdateSourceTrigger=PropertyChanged}" Grid.Row="3" />

        <Label Content="Country:" Grid.Row="4"/>
        <TextBox x:Name="CountryTextBox" Text="{Binding Country, UpdateSourceTrigger=PropertyChanged}" Grid.Row="4" />

        <Button Content="Update Address" Grid.Row="5" Click="UpdateAdressButton_Click" HorizontalAlignment="Right"/>

    </Grid>
</Window>
